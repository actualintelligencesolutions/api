-- Step 1 seed: create the very first super_admin manually.
-- Replace the pin hash with one generated by PHP's password_hash('1234', PASSWORD_DEFAULT)
-- Example:
-- php -r "echo password_hash('1234', PASSWORD_DEFAULT), PHP_EOL;"
--
-- After this first login works, you can import a full org bootstrap payload such as:
-- database/imports/subansiri-bootstrap.example.json

INSERT INTO users (
    employee_code,
    full_name,
    mobile,
    email,
    role,
    pin_hash,
    status,
    joining_date,
    created_at,
    updated_at
) VALUES (
    'SUB100',
    'Subansiri Admin',
    '9946580637',
    'subansiri.admin@example.com',
    'super_admin',
    '1234',
    'active',
    CURDATE(),
    UTC_TIMESTAMP(),
    UTC_TIMESTAMP()
);
